//Accept one character from the user convert case of that character

void DisplayConvert(char cValue)
{

}
#include<stdio.h>
int main()
{
    char cValue=0;
    printf("Enter the character:");
    scanf("%d",&cValue);

    DisplayConvert(cValue);
    return 0;
}